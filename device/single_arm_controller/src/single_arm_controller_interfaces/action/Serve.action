# Goal
string wrist_cam_path    # wrist camera device path (e.g. /dev/video8), empty = use default
string realsense_serial  # RealSense serial number, empty = use default
string task              # task instruction, empty = use default
---
# Result
bool success
string message
---
# Feedback
string status
