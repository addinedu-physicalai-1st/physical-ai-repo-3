# Documents
/doc/
.doby

# markdown
**/AGENTS.md

# === ROS2 build artifacts ===
build/
install/
log/
src/service/moca_service/.ros_ws/

# === Python ===
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.venv/

#service
**/tests/

# === IDE ===
.vscode/
.idea/
.antigravitycli/
.agents/
*.swp
*~

# === OS ===
.DS_Store
Thumbs.db

# === Project temporary ===
*.log
*.bag
rosbag2_*/

# === Heavy assets (Phase 0-A에서 가져온 cabot 자산) ===
# 이 디렉토리들은 별도 관리 (git-lfs 또는 다운로드 스크립트)
**/maps/
**/datasets/
**/web/
**/tools/
**/models/
!src/service/web_service/app/models/
!src/service/web_service/app/models/**
!src/service/web_service/app/models/**
!src/controller/ddooby_controller/assets/manufacturing_world/models/
!src/controller/ddooby_controller/assets/manufacturing_world/models/**

# OpenArm CAN upstream Launchpad release packaging encrypted files; not needed for this demo.
src/controller/ddooby_controller/openarm_vendor/openarm_can/packages/launchpad-account.asc.kou
src/controller/ddooby_controller/openarm_vendor/openarm_can/packages/release-key.asc.kou

# ddooby local modeling workspace.
# Gazebo runtime uses assets/manufacturing_world/layout.json and generated model.sdf files instead.
src/controller/ddooby_controller/modeling/

# === ML model assets (mediapipe .task/.tflite 등) — download_models.sh 로 별도 관리 ===
*.pt              # YOLO weights
*.pth             # PyTorch checkpoints
*.onnx
*.task
*.tflite
*.gguf            # LLM weights
*.bin
# audio assets (TTS reference 등) — 별도 다운로드 스크립트로 관리
*.wav

# === Data ===
*.csv
*.xlsx
*.npz
*.npy

# === Docs that are reference only ===
docs/cabot_legacy/
docs/cabot_README.md

# === Submodule git directories (vic_pinky는 자체 git) ===
# .git 디렉토리 자체는 ignore 하면 안 됨 (vic_pinky/.git이 함께 사라짐)
# 그러나 vic_pinky 내부 변경사항은 vic_pinky의 자체 git이 관리

# === src/shared 의 외부 저장소 (각자 자체 git, vcs import로 복원) ===
src/shared/vic_pinky/

# Antigravity
.antigravitycli

# mkcert 로컬 인증서 (개발 환경, 절대 commit 하지 말 것)
src/service/certs/
# clone 직후에도 docker-compose mount 가 깨지지 않도록 빈 디렉토리만 보존
!src/service/certs/.gitkeep

# docker-compose 환경변수 (HTTPS cert 경로 등 로컬 설정)
src/service/.env
