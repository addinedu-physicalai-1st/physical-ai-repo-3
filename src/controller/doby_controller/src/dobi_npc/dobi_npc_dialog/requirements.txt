# dobi_npc_dialog — Python pip 의존성 핀 (Phase 2 W4-③)
#
# 설치:
#   pip install --user -r requirements.txt
#
# 시스템 apt (rosdep 인식, package.xml에 명시됨):
#   python3-yaml, python3-pygame, python3-pil, python3-numpy
#
# 인터넷 의존: edge-tts는 Microsoft Edge 클라우드 TTS API 사용.
# 오프라인 환경 대응은 Phase 후속(piper 등)에서.
#
# 미래 LLM/TTS 엔진 변경 가능성: 다른 팀원 조사 중. 변경 시
# 본 파일 + tts_node.py + persona YAML voice 섹션 + CLAUDE.md §7 갱신.

edge-tts==7.2.7
