# moca 클라이언트 (노트북 3 + 로봇 2) — 5090 서버를 master로
# 배치: /etc/chrony/conf.d/moca.conf  (또는 /etc/chrony/chrony.conf 끝에 추가)

# 1순위 — 5090 LAN 마스터
server 192.168.0.133 iburst prefer

# 백업 — 인터넷 가능 시 (5090 단독 의존 회피)
pool kr.pool.ntp.org iburst minpoll 8

makestep 1.0 3
rtcsync
