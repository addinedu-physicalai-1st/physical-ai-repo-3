# Legacy Beverage Test

This directory keeps the previous Gazebo beverage test prototype for reference.

It is not part of the `ddooby_controller` runtime build or install targets. The
current manufacture flow uses `hotdog_making.launch.py` with `task:=hotdog`,
`task:=coke`, or `task:=coffee`.
